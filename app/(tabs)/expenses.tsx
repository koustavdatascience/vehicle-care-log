import { PhasePlaceholderScreen } from "@/components/screens/phase-placeholder-screen";
export default function ExpensesScreen() { return <PhasePlaceholderScreen title="Expenses" icon="chart.pie.fill" message="Expense totals, categories, and reports are implemented after the local record workflows are ready." />; }
